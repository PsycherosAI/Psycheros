---
// Empty override — Psycheros docs are dark-only by brand decision.
// The Psycheros app UI itself is dark-only (true-black OLED), and the
// docs match. To re-enable the light/dark toggle, delete this file
// and remove the `components.ThemeSelect` entry from astro.config.mjs.
---
